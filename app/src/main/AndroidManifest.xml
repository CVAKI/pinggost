<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- WiFi scan results require location permission on Android 6.0+ -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.sensor.magnetic_field" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Ping Ghost"
        android:theme="@style/Theme.PingGhost"
        android:supportsRtl="true">

        <activity
            android:name=".SplashMeltActivity"
            android:exported="true"
            android:theme="@style/Theme.PingGhost.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.PingGhost.NoActionBar" />

        <activity
            android:name=".ScaryActivity"
            android:exported="false"
            android:theme="@style/Theme.PingGhost.NoActionBar"
            android:excludeFromRecents="true" />

        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:theme="@style/Theme.PingGhost.NoActionBar"
            android:excludeFromRecents="true" />

    </application>

</manifest>